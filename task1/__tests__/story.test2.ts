describe('User want to register in library system', () => {
  test('User was added to library', () => {
    expect(true).toBe(true);
  });
});

describe('User want to rent a book and return it before deadline', () => {
  test('check is user registered in library system', () => {
    expect(true).toBe(true);
  });

  test('check is book avaliable in library system', () => {
    expect(true).toBe(true);
  });

  test('rent book for user', () => {
    expect(true).toBe(true);
  });

  test('check is user return book in time', () => {
    expect(true).toBe(true);
  });

  test('return book for user without paying fee', () => {
    expect(true).toBe(true);
  });
});

describe('User want to rent a book and return it after deadline', () => {
  test('check is user registered in library system', () => {
    expect(true).toBe(true);
  });

  test('check is book avaliable in library system', () => {
    expect(true).toBe(true);
  });

  test('rent book for user', () => {
    expect(true).toBe(true);
  });

  test('check is user return book in time', () => {
    expect(true).toBe(true);
  });

  test('return book for user with paying fee', () => {
    expect(true).toBe(true);
  });
});

describe('Librarian want to add new book to Library system and delete it after x days', () => {
  test('create new book in system', () => {
    expect(true).toBe(true);
  });

  test('add created book to library system', () => {
    expect(true).toBe(true);
  });

  test('check is book avaliable in library system', () => {
    expect(true).toBe(true);
  });

  test('delete book from library after x days', () => {
    expect(true).toBe(true);
  });
});

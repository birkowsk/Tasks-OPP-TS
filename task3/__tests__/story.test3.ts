describe('User want to create account in App', () => {
  test('add User to App', () => {
    expect(true).toBe(true);
  });
});

describe('super user want to change some data for user', () => {
  test('change users password', () => {
    expect(true).toBe(true);
  });
  test('change users access level', () => {
    expect(true).toBe(true);
  });
  test('change other users password', () => {
    expect(true).toBe(true);
  });
});
